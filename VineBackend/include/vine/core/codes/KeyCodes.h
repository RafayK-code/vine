#pragma once

#include <SDL.h>

namespace sntl
{
    using KeyCode = uint16_t;

    namespace Key
    {
        enum : KeyCode
        {
            Space = SDLK_SPACE,
            Quote = 
        };
    }
}